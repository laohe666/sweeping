<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.laohe.dao.HistorySiteDao">
    <select id="selectByUrl" parameterType="string" resultType="string">
        select url from history_site where url = #{url}
    </select>
</mapper>